<%= form_with(model: gweet, local: true) do |form| %>
  <% if gweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gweet.errors.count, "error") %> prohibited this gweet from being saved:</h2>

      <ul>
      <% gweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post %>
    <%= form.text_area :post %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
